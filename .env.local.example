# Frontend (Vite) — created by `npx convex dev`, points the React app at your deployment.
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# ---------------------------------------------------------------------------
# The keys below are NOT read by the frontend. Set them on the Convex backend:
#   npx convex env set OPENAI_API_KEY sk-...
#   npx convex env set HIGGSFIELD_API_KEY ...
#   npx convex env set HIGGSFIELD_BASE_URL https://api.wavespeed.ai/api/v3
# Listed here only as a checklist of what the backend needs.
# ---------------------------------------------------------------------------
# OPENAI_API_KEY=
# HIGGSFIELD_API_KEY=
# HIGGSFIELD_BASE_URL=
